/*
    Operator-->
        1-Bitwise AND = &&
                -- IF all the number is true then the condition will true
                -- If one condition get false then total condition get false
                -- 0-False and 1-True
                -- When you AND(&) 1 with any number, digit remains same
        2-Bitwise OR = ||
                -- IF one number or condition is true then the condition will true
                -- If two condition get false then total condition get false
                -- 0-False and 1-True
        3-XOR = (if and only if)
                -- In this condition only one is true then the condition is true
                -- If 2 false or 2 true arrive then the condition get false
                -- 0-False and 1-True
                -- Anything XOR with 1 , then it will return the opposite of the number
                -- a XOR a = 0
                -- Ex-
                    Explanation : A = 7 ( 0111 )2 , B = 9 ( 1001 )2
                    1st bit of A = 1 and B = 1 so XOR value will be 0
                    2nd bit of A = 1 and B = 0 so XOR Value will be 1
                    3rd bit of A = 1 and B = 0 so XOR Value will be 1
                    4th bit of A = 0 and B = 1 so XOR Value will be 1
                    XOR value has binary representation :  (1110)2
                    Decimal value of XOR = 14
        4-Complement = ~
                -- a = 1 0 1 1 0 => ~a = 0 1 0 0 1
                -- Means a = 1 => ~a = 0


 */

public class Bit_Manipulation {
    public static void main(String[] args) {

    }
}
